# Example server environment variables (do NOT commit secrets).
# For Atlas SRV URIs: if your password contains special chars encode it (e.g. @ -> %40).
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/snackmaster?retryWrites=true&w=majority
# or local fallback for dev:
# MONGO_URI=mongodb://127.0.0.1:27017/snackmaster

JWT_SECRET=replace_this_with_a_secure_value
NODE_ENV=development
PORT=5000